version: '3'
services:
  react-app:
    build:
      context: .
      dockerfile: docker/Dockerfile
    container_name: react-app
    ports:
      - "3000:80"
    environment:
      - NODE_ENV=production
    restart: always